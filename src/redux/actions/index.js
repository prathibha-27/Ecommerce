export const UPDATE_PRODUCT_LIST = (val) => ({
  type: "UPDATE_PRODUCT_LIST",
  val,
});
export const UPDATE_PRODUCT = (val) => ({
  type: "UPDATE_PRODUCT",
  val,
});

export const UPDATE_CART = (val) => ({
  type: "UPDATE_CART",
  val,
});

export const UPDATE_ADDRESS = (val) => ({
  type: "UPDATE_ADDRESS",
  val,
});

export const CLEAR_ADDRESS = (val) => ({
  type: "CLEAR_ADDRESS",
  val,
});

export const UPDATE_PICKUP = (val) => ({
  type: "UPDATE_PICKUP",
  val,
});

export const DISPLAY_MINICART = (val) => ({
  type: "DISPLAY_MINICART",
  val,
});

export const DISPLAY_SEARCH = (val) => ({
  type: "DISPLAY_SEARCH",
  val,
});
